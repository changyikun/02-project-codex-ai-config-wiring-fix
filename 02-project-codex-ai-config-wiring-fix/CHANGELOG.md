# Changelog

## 维护规则

- 后续 changelog 按版本更新，不再按日期新增章节。
- 新增内容只写入最新版本章节；除格式迁移外，不回改旧版本章节。
- 如果最新版本中后续修正了该版本里已经写过的条目，不覆盖原条目原文；在最新版本末尾追加修订记录，格式为：`版本号 - 修改内容简述 - 见第 N 条`。
- 每个版本内的主要变更使用编号条目，便于修订记录引用。

## v0.5.5 - 后续系统迭代（进行中）

> 当前开发版本。后续新增内容只写入本版本；`v0.5.4` 已归档，除必要勘误外不再追加。

1. 归档 `v0.5.4 - 后续系统迭代`，后续系统修改进入 `v0.5.5`。
2. 妙音堂场景重构：妙音堂一级子场景只保留 `宴席报名` 与 `闲逛` 两个地点按钮；`闲逛` 接入随机事件系统，从 `location.miaoyin.common`、`location.miaoyin.music`、`location.miaoyin.dance` 三个池合并抽取，每次先结算压力 `-2`，低权重事件可拾取物品。乐师连翘与新增舞者凌袖不再开局常驻显示，而是分别在乐曲池 / 舞蹈池事件被触发多次后出现在场景 NPC 区；二者均复用 `AudienceInteractionShell`，支持送礼、闲聊和请求指导。连翘指导推进曲谱学习并增加乐理，凌袖指导推进舞曲练习进度并增加气质；相关固定对白写入 `npc_tools_dialogues.csv`，闲逛事件写入 `random_events.csv`。
3. 大地图热点竖牌样式修复：`map-main__hotspot is-vertical` 不再把竖牌素材挂在按钮外框上，按钮本体改为透明，素材只绘制在内部直接子元素；竖牌宽度按 `shukuang1.png` 的真实比例由高度计算，避免外层方形容器和竖牌被横向压扁。同步收窄热点文字选择器，只作用于直接子元素，避免每个文字字符继承外层热点尺寸。
4. v0.5.5 - 大地图热点选中态修订：选中 / 悬停时不再给 `map-main__hotspot` 按钮本体添加滤镜阴影，避免浏览器按按钮边界绘制外层方形框；高亮效果改为只作用于内部竖牌素材，并显式清除按钮 focus / active 的 outline 与 box-shadow。本条修订第 3 条。
5. HBuilderX / App 构建方式：保留现有 `build:web` 输出 `dist/`，新增 `build:app` 输出 `dist-app/`。Vite 在 `app` mode 下使用 `base: './'`，并通过 `scripts/prepare-hbuilder-build.cjs` 后处理构建产物，把 HTML / JS / CSS 中残留的 `/assets/...` 根路径改为 HBuilderX WebView 可解析的相对路径；若仍存在根路径资源引用，构建会直接失败。新增 `preview:app` 用于本地预览 `dist-app/`，并将 `dist-app/` 加入 `.gitignore`。
6. Windows exe 打包：新增 Electron 桌面外壳，入口为 `electron/main.cjs`，加载 `build:app` 生成的本地 `dist-app/index.html`；新增 `build:exe` 脚本，通过 `scripts/package-electron-dir.cjs` 组装 Windows x64 可执行目录 `release/fenghualu-win-x64/`，其中 `凤华录.exe` 可直接运行。桌面包不启动开发服务器，当前玩法继续使用本地 CSV / 前端状态流。
7. v0.5.5 - 妙音堂闲逛事件修订：删除第 2 条中 6 条短占位闲逛事件，改用玩家提供的 15 条完整妙音堂闲逛事件，保留原文信息量、选项分支和拾取物；默认 `压力 -2` 不再由入口统一先扣，而是写入每条事件的 `effectJson`，入口只在未抽到事件时走兜底收益，避免事件收益和入口收益重复结算。新增旧黄纸平安符、兰草白玉簪、缠枝小铜铃、兰花绢帕、钱塘琵琶拨片等可拾取礼物道具；同时修复随机事件进入选项阶段时清空当前剧情行导致对话层消失、事件无法正常收束的问题。本条修订第 2 条。
8. v0.5.5 - 失物归还彩蛋扩展：妃嫔送礼入口的银叶耳坠归还判定泛化为失物归还 runtime，保留耳坠原有隐藏失主 metadata，同时让妙音堂“兰花绢帕”在拾取时生成带隐藏失主、提示字和独立 itemId 的实例物品。玩家可出售或送给任意人；只有送还给 metadata 记录的失主时，才播放 `relationship_audiences.csv` 的帕子归还特殊对白，并在普通礼物好感之外额外增加好感。本条修订第 7 条。
9. v0.5.5 - 妙音堂拾取物属性修订：钱塘琵琶拨片改为任务物品，不能出售、不能被杜娘回收，也不会出现在妃嫔 / 太后 / 常驻 NPC 送礼列表中；其后续用途保留给后续剧情或彩蛋入口。本条修订第 7 条。
10. 非妃嫔 NPC 配置化：新增 `src/game/npcs/csv/non_consort_npc_profiles.csv` 与 `npcCatalog`，皇帝容安、太后、杜娘、李公公、月姑姑、娇娇、通用内侍 / 宫人，以及阿翎、布自游、简宁、凌萧、凌袖、当一、卢安平等非妃嫔资料统一从 CSV 读取。情缘系统改为读取 `isRomanceable=true` 的非妃嫔 NPC，凌袖标记为可攻略，旧 `src/config/npcs.ts`、旧 `npc-*` ID、旧 `连翘` NPC 配置和旧 `bondNpcUnlocked:lianqiao` flag 停用；妙音堂乐师状态改名为 `musician*`，旧存档结构不做兼容迁移。
11. v0.5.5 - 妙音堂谱子系统修订：新增 `dance-score` 背包分类和 `inventory_items.csv` 舞谱池，凌袖请求指导不再推进单一 `dancePracticeProgress`，而是必须选择持有舞谱并复用曲谱练习公式写入 `musicHallProgress.danceScoreMastery`；凌萧 / 凌袖关系好感每跨过 `global_numeric_rules.csv` 的 `miaoyin_score_reward_affinity_step` 档位，会获得一张对应随机曲谱 / 舞谱。旧寝殿凌萧隔旬自动赠谱流程停用，存档 schema 提升到 `7`，旧舞曲进度结构不做迁移。本条修订第 2、10 条。
12. v0.5.5 - 妙音堂初见赠谱修订：玩家首次点击凌萧 / 凌袖进入人物交互页时，会随初见对白获得一张对应随机曲谱 / 舞谱；该赠谱只按常驻 NPC 首次结识触发一次，后续仍由第 11 条的好感跨档规则继续发放。本条修订第 11 条。
13. v0.5.5 - 凌萧随机闲聊池修订：按用户提供文本将凌萧初遇、通用闲聊、低好感闲聊、高好感闲聊和琵琶拨片后续写入 `random_events.csv`；凌萧初遇改为随机事件分支演出，并通过 `{{scoreName}}` 承接本次赠送曲谱。凌萧闲聊从通用池和当前好感池合并抽取，闲聊收益通过 `target.relationToPlayer` 映射到常驻 NPC 好感；琵琶拨片后续只依赖前置事件 `miaoyin.music.pipa-pick`，不再由组件检查背包或额外开关。本条修订第 7、11、12 条。
14. v0.5.5 - 凌萧初遇归固定剧情修订：凌萧初遇不再作为随机事件维护，改回 `npc_tools_dialogues.csv` 的固定剧情；`random_events.csv` 只保留凌萧通用闲聊、低 / 高好感闲聊和琵琶拨片后续。初遇仍会发放随机曲谱，但不参与随机抽取、不写入随机事件触发计数。本条修订第 13 条。
15. v0.5.5 - 大地图左侧面板返回入口修订：删除大地图工具面板外层额外的 `返回地图` 按钮，左侧入口打开的个人属性、嫔妃、纪事、情缘等面板只保留各自面板内部的返回入口；返回后仍停留在大地图，不切回寝殿。
16. v0.5.5 - 凌袖固定初遇与随机闲聊池补充：按用户提供文本替换 `npc_tools_dialogues.csv` 中凌袖固定初遇与初见 / 关系赠舞谱文本；`random_events.csv` 新增凌袖低好感、通用、高好感三类闲聊池共 13 个事件，保留分支对白并保证每个选项都有常驻 NPC 好感收益。妙音堂人物闲聊入口改为按 NPC ID 组装通用池 / 好感池，不再只为凌萧写死池子。本条修订第 11、12、14 条。
17. v0.5.5 - 日历翻页动效：右上时间状态块在年月、旬或时辰变化时，会以“新日历页从顶部翻下并覆盖旧页”的方式播放局部动画；银两与体力状态条不参与翻页，首次渲染和场景切换重挂载不触发动画。动画仅为 UI 反馈，不阻塞玩家操作；连续时间变化时只保留最新时间，并尊重系统减少动态效果设置。
18. v0.5.5 - 日历翻页视角修订：调整第 17 条翻页动画的透视原点、初始深度和 `rotateX` 方向，使新页表现为从旧日历后上方翻到前景，而不是从日历内部向外翻出。本条修订第 17 条。
19. v0.5.5 - 日历翻页流畅度修订：将第 17、18 条的翻页关键帧改为单段连续扣落，移除明显的中途回弹和两段式停顿；同步延长动画与 overlay 清理时长到 `620ms`，让高光、暗边和落页节奏一致。本条修订第 17、18 条。
20. v0.5.5 - 日历翻页二段感修订：进一步删除第 19 条中段角度关键帧和底页 dim 动画，让翻页从起点到终点只做一次连续插值；overlay 清理时长同步为 `640ms`，避免出现“翻下后再归位”的二段式观感。本条修订第 17、19 条。
21. v0.5.5 - 御花园闲逛事件：御花园场景一级按钮收口为 `闲逛`，不再保留旧 `游园 / 听风` 两个泛用行动；`GenericMapLocationView` 为御花园接入 `location.imperial-garden.stroll` 随机事件池，使用 seeded 抽取和随机事件对话舞台，每条事件默认 `压力 -2`，拾取类选项可获得缠枝莲玉佩、兰枝绣帕、淡珠如意结、素白玉簪等独立礼物物品。事件文本按用户提供原文拆行写入，不做简写；随机宫殿引用由入口传入变量。本条修订第 2、7 条的地点随机事件口径。
22. v0.5.5 - 剧情触发顺序锁：新增 `activeBlockingNarratives` 临时 UI 锁和 `useBlockingNarrativeLock`，妙音堂、御膳房、宫门和泛用地点随机事件在播放期间会阻塞夜晚侍寝通报、侍寝事件和过夜黑屏 overlay 的渲染。夜晚事件仍可先写入 store pending，但必须等当前随机事件完全收束后再展示，避免太监立绘和当前剧情舞台同屏叠加。本条修订第 2、7、21 条的随机事件演出口径。
23. v0.5.5 - 纪事系统五分类：纪事面板改为 `圣旨 / 闲言 / 秘事 / 事件 / 内务` 五类，每条记录显示对应年月旬日期；`SettlementReport` 增加 `chronicleCategory` 与 `chronicleOnly`，晋封旨意进入圣旨，宫宴等系统流程进入事件，月俸 / 用度 / 月结算进入内务。妃嫔之间旬级互动造成关系变化时写入闲言，皇上夜间侍寝 / 伴驾 / 独寝情况也写入闲言；这些闲言为纪事留档，不触发清晨通报弹窗，也不会抢占娇娇 / 太监通报队列。
24. v0.5.5 - 纪事展示修订：娇娇旬报 / 月报统一归入 `内务`，不再作为普通事件显示；纪事页把 `SettlementReport.lines` 按行拆成独立记录展示，月初通报、月俸、用度、声望等不再合并在同一张明细列表中。没有真实记录的分类保持空白，不再生成“暂无事件 / 暂无新记”类占位纪事。本条修订第 23 条。
25. v0.5.5 - 纪事信息密度修订：宫宴报名通知改为按通知发生时间留档，正文只保留“宫宴报名开启通知：本届宫宴定于……”这一条提示；清晨通报不再写体力恢复和侍寝保底等内部机制值，熬夜惩罚文案读取数值表真实值。月初内务把月俸 / 用度 / 银两合并为一条，把位分 / 声望合并为一条，无宫斗案件时不生成占位记录；闲言删除“关系走向在旬末收束”这类实现说明。本条修订第 23、24 条。
26. v0.5.5 - 通报与纪事正文拆分：`SettlementReport` 新增 `chronicleLines`，`lines` 继续作为娇娇 / 太监当场通报正文，纪事页优先读取 `chronicleLines`。清晨通报删除“已入某月某旬清晨”这类空信息句，恢复体力值恢复与侍寝保底通报；这些机制结算只保留在娇娇通报中，不写入纪事。本条修订第 24、25 条。

## v0.5.4 - 后续系统迭代（已归档）

> 已归档。v0.5.4 主要完成地图子场景通用交互、常驻 NPC 与随机事件入口扩展，并统一人物交互外壳、场景 NPC 立牌、地点行动按钮和外景回宫 / 离开语义。版本后段重点补齐御膳房、宫门杜娘、建章宫太后、皇帝日间会面与养心殿李公公流程，推动随机事件、物品 tag、常驻 NPC 关系和皇帝日程从临时逻辑转为可维护系统。

1. 归档 `v0.5.3 - 后续系统迭代`，后续系统修改进入 `v0.5.4`。
2. 地图子场景通用交互框架：新增 `MapSubsceneView`，统一所有进入地点后的一级布局；场景中间偏上展示固定 NPC、游走妃嫔和特殊人物，右侧只展示地点行动 / 业务入口，底部固定 `离开` 返回大地图。御膳房、妙音堂、太医院、宝华殿、华清池、掖庭院、宫门、建章宫和泛用地点均迁入该框架，旧的地点一级入口卡片和 `LocationConsortVisitorsPanel` 拆除，复杂买卖、学谱、问诊、礼佛、泡汤和太后 / 皇帝流程继续作为二级业务保留。
3. 测试用例清理：扫描全量测试，移除或收口与当前规则不一致的旧断言，包括初始背包非空、旧地点剧情分页文案、旧剧情说话人硬编码、旧作品售价阈值、旧深夜熬夜 UI 集成流和旧他人侍寝清晨通报时序；保留当前仍有效的购买 / 售卖、送礼、侍寝 runtime、影落掖庭状态流转和存档分桶覆盖。
4. 子场景视觉与入场流程修订：右侧地点行动按钮组改为垂直居中并放大按钮尺寸；场景 NPC 区改为更大的两排三列立绘 + 名字标签组合，并整体下移到画面中段。进入地图子场景时不再自动播放“娇娇打点通传”类入场对白，玩家直接进入地点交互层，只有点击 NPC 或地点行动后才触发对应剧情。
5. 子场景 NPC 展示二次修订：取消 `MapSubsceneView` 的 NPC 卡片式底板，改为透明点击热区上的人物立牌；名牌统一为 `官职/位分·名字`，不再额外显示底部身份行。立牌整体继续放大并改为左对齐立绘，名牌使用深色描边、浅色填色、弱阴影的扁平风格，后续同类场景名牌按此视觉方向延续。
6. 子场景 NPC 排布修订：NPC 六个槽位改为底排优先、再向上补位；立牌区域从固定像素改为随视口宽高自适应放大，宽屏下尽量撑满场景中间区域，同时保留右侧行动栏的安全空间。
7. 后宫妃嫔拜访入口修订：从后宫殿位进入妃嫔日常对话只切换到对方寝宫场景，不再调用耗时地点行动流，也不推进时辰；体力不足拦截、会客结束后 NPC 回归和会面操作次数限制继续保留。
8. 宫门杜娘与常驻 NPC 关系修订：删除与 `养心殿` 重合的重复地图地点，相关剧情和行动入口统一并入 `养心殿`；杜娘改为常驻 NPC 关系模型，初见对白读取 `npc_tools_dialogues.csv`，闲谈接入 `random_events.csv` 的杜娘事件池，送礼 / 闲谈消耗每旬交互次数，买卖不耗次数。杜娘只售卖中低品质宫外物件，收购所有非任务物品；物品表新增 `isQuestItem` 标记任务物品，高好感解锁买入折扣与回收加价。随机事件的 `unlockEventIds` 改为排入 `pendingUnlocks`，到下一旬清晨后才进入可抽池；存档 schema 提升到 `5` 并保存 `progress.randomEvents` 与 `relations.permanentNpcRelationships`。
9. 杜娘随机事件池扩充：`random_events.csv` 中杜娘闲谈扩展为 20 个事件，覆盖通用池、低好感池和高好感池；对白改为更贴近宫门小贩的买卖、跑腿、家信、熟客、回收边界和友情价等日常内容，减少无意义环境描写，保留自然分支选项与好感 / 压力 / 银两等局部效果。
10. 人物交互 UI 统一：新增 `AudienceInteractionShell` 作为妃嫔、常驻 NPC 等人物交互页的共享外壳；`ConsortAudiencePanel` 和宫门杜娘 / 阿翎入口均改为消费同一套 header、状态卡、立绘区、右侧操作栏、选择面板和对话舞台结构。杜娘买卖 / 送礼面板不再使用宫门专用交互卡片，而是挂入共享人物交互页的 picker 区。
11. 人物交互 UI 安全区修订：共享人物交互页新增统一安全边距变量，左侧状态卡整体右移避开左侧菱形功能栏，右上返回按钮移到时间状态栏左侧，右侧操作按钮从状态栏下方开始排列；太后、妃嫔、杜娘 / 阿翎等消费 `AudienceInteractionShell` 的交互页同步生效。
12. 杜娘交互页收束修订：杜娘 / 阿翎人物页不再把买卖、送礼、次数不足等业务反馈塞进全屏对话锁，业务面板保持可点击；杜娘普通入场对白只播放一段，工具 NPC 对话关闭自动引号拆段，避免每次交互被拆成两段。宫门人物页退出文案统一为 `返回`，不再单独维护 `返回宫门`。
13. 杜娘右侧提示清理：移除杜娘人物页右侧操作栏中互动后追加的临时提示标识；右侧栏只保留交互次数和操作按钮，购买 / 售卖 / 送礼结果不再作为额外文字塞入按钮组。
14. 杜娘对白文风修订：对照古言剧本参考，将杜娘初见、买卖、回收和随机闲谈从 AI 式抽象旁白改为更适合游戏对白框的直接台词；保留宫门小贩的价目、货源、账目、人情和交易边界信息，减少额外动作 / 环境描写，不改随机事件分支、效果和解锁结构。
15. 古言对白参考文本整理：清理外部参考文件 `古言对白收录.md` 中对白行内的括号动作说明，并将单独成行的括号场景 / 动作说明改写为 `旁白：...` 格式，便于后续从剧本文本中抽取可用于游戏对白模块的说话人和正文。
16. 杜娘随机事件用户稿替换：删除上一版 20 条杜娘闲谈事件，改用用户提供并补齐效果的 25 条事件；事件池拆为通用 12 条、低好感 4 条、高好感 9 条，每次闲谈至少结算一次杜娘好感收益，有选项的事件每个选项也保证正向好感收益。本条修订第 9、14 条中旧事件池口径。
17. 杜娘交互页称谓与返回入口修订：随机事件中玩家发言的身份栏改为读取 `{{playerRank}}`，杜娘运行时传入玩家当前位分，不再显示“玩家”作为称号；宫门工具 NPC 人物页取消共享外壳顶部返回按钮，只保留右侧操作栏中的 `返回`，避免同一页面出现两个退出入口。本条修订第 10、12、16 条。
18. 属性说明弹窗：`player_attribute_fields.csv` 的 `description` 改为玩家可见属性说明，逐项说明属性含义和影响玩法；属性创建页每个属性名后新增 `?` 圆形按钮，点击后在当前位置弹出小说明框，运行时只读取属性配置，不在组件内维护第二份说明文案。
19. 嫔妃属性说明弹窗：新增 `consort_attribute_fields.csv` 维护嫔妃声望、宠爱、野心、好感、倾情等核心属性说明；嫔妃总览面板每个属性名后新增 `?` 小按钮，说明弹窗同样只读取数值配置表，不在组件内硬编码第二份属性解释。
20. 随机事件玩家立绘修复：`random_events.csv` 中玩家发言行统一补 `portraitKey=player` / `portraitPlacement=stage`，运行时渲染随机事件行时同步渲染 `portraitKey` 模板；宫门杜娘闲谈在随机事件轮到玩家发言时，会把共享人物页立绘切换为当前路线玩家立绘，不再继续显示杜娘常驻立绘。本条修订第 16、17 条。
21. 属性说明文案与局内查看修订：玩家 / 嫔妃属性说明改为更概括的玩家可见口径，不再暴露过细的系统名和判定细节；局内个人属性面板的主属性与技艺属性也接入 `player_attribute_fields.csv` 的 `?` 说明弹窗，和创建页共用同一份配置文案。本条修订第 18、19 条。
22. 属性说明浮层与缺项补齐：新增 `player_status_fields.csv` 维护声望、宠爱、野心、家世、压力、子嗣等局内非加点状态说明；属性说明按钮统一改用 `AttributeHelpButton` 固定浮层，弹窗渲染到页面顶层并按按钮位置自动选择左 / 右侧，避免被面板层级或屏幕边缘遮挡。本条修订第 18、19、21 条。
23. 御膳房内容扩展：御膳房购买食物改为读取 `kitchen_shop_offers.csv` 的季节供货表，按当前月份随机刷新，保留常备食物并对每个商品执行每旬限购；`inventory_items.csv` 补充青梅、枇杷、鲜枣、蜜柿、桑葚、野蜜、栗子等食物和闲逛可获得物品。御膳房 `闲逛` 接入 `random_events.csv` 的 `location.kitchen.stroll` 事件池，当前共 19 条事件，每次闲逛至少获得压力 `-2`，部分分支可获得银两或物品；第四次闲逛强制结识布自游的剧情优先级保持不变。
24. v0.5.4 - 御膳房随机事件与物品 tag 修订：补回玩家提供的御膳房闲逛事件信息量，不再把事件压缩成一句短文；`inventory_items.csv` 新增通用 `tags` 字段，`inventoryTagRuntime` 按 tag 稳定抽取物品，随机事件的 `effectJson` 可通过入口变量引用抽中的 `itemId`。`【低品质食物】`、`【树上果实】` 等占位不再写成场景特例，而是由通用物品 tag 系统解析为具体物品。本条修订第 23 条。
25. v0.5.4 - 御膳房随机事件固定收益修订：御膳房闲逛事件不再只有压力恢复和拾取物收益；公开善行、帮人遮掩、识字看账、辨物观察等分支按事件内容固定增加声望、政治、心计、气质、药理等属性，不做随机收益判定。私拿食物、捡钱和藏物类分支仍只给物品、银两或健康收益，不额外加声望。本条修订第 23、24 条。
26. v0.5.4 - 御膳房随机抽取修复：修复御膳房闲逛连续多次只会抽到同一事件的问题；原入口使用简单字符串求和 hash，连续 `stroll` 计数落在相同权重区间。随机事件 runtime 新增统一 seeded 抽取 helper，御膳房入口改用该 helper，并增加连续种子至少能抽出多个事件的回归测试。本条修订第 23、24 条。
27. v0.5.4 - 随机事件抽取统一：杜娘闲谈不再在 `GongmenView` 中使用 `Math.random` 和私有权重抽取；随机事件 runtime 新增多事件池合并抽取 helper，杜娘通用池 + 好感池与御膳房闲逛都改为走同一套 seeded 随机事件抽取能力，并补充杜娘合并池回归测试。本条修订第 8、16、26 条。
28. v0.5.4 - 银叶耳坠失主彩蛋：御膳房闲逛的银叶耳坠不再是固定道具；收起时会从当前存活妃嫔中稳定选择隐藏失主，物品描述只显示失主姓名中的一个提示字。随机事件 `inventory.gain[]` 新增 `templateItemId / description / metadata`，用于从物品表模板生成同名独立实例；玩家仍可出售或送给任意人，只有通过妃嫔送礼入口送还给 metadata 中记录的失主时，才触发 `relationship_audiences.csv` 的特殊归还对白，并在普通礼物好感之外额外增加好感。本条修订第 23、24 条。
29. v0.5.4 - 建章宫太后互动闭环：太后改为常驻 NPC 关系对象并复用 `AudienceInteractionShell`，建章宫清晨到傍晚可拜见，夜晚 / 深夜只播宫人通传不进入交互也不耗次数。太后每旬有效互动上限为 `2` 次；`请安` 每旬最多一次，`请安` 与 `送礼问安` 均记入月度问安义务，送礼复用妃嫔礼物筛选且只结算礼物好感。若上月未向太后问安，次月月初由建章宫掌事传话并扣声望 `20`；相关次数、太后好感和月度问安标记保存在 `relations.permanentNpcRelationships.dowager`。
30. v0.5.4 - 人物交互退出耗时规则：妃嫔、公共地点偶遇妃嫔、杜娘等常驻 NPC、太后和皇帝日间互动在本次会面实际消耗至少 `1` 次交互 / 主行动后，主动返回或送客退出人物交互页时统一推进 `1` 个时间格；买卖、选礼、单纯进入退出、夜间拒见和未耗次的告退不推进时间。该推进复用地点行动的深夜 / 体力归零睡觉流程，避免深夜退出人物页直接跳过黑屏转场。
31. v0.5.4 - 皇帝日间交互页拆分：皇帝公共偶遇作为地图子场景中间 NPC 立牌触发，不再进入侍寝式界面；日间求见 / 偶遇改为复用 `AudienceInteractionShell` 的专用交互页，并新增独立 `emperorDayAudienceRuntime` 维护研墨、奉茶、问安、闲谈、对弈、进言等日间动作。按摩、撒娇、入怀等侍寝动作不再出现在日间会面；每次成功会面有 `2` 次有效互动，耗次后退出推进时间，养心殿求见失败则返回当前子场景且不推进时间。本条修订第 22、23、25、30 条。
32. v0.5.4 - 养心殿主场景重构：养心殿不再使用泛用地点行动按钮，也不再直接提供“求见皇上 / 朝堂事务 / 抄读”等场景按钮；进入后只保留常驻 NPC 太监总管李公公和可能出现的游走妃嫔。李公公接入常驻 NPC 关系，可求见皇帝、送礼、打点银两和闲聊；好感会提高养心殿求见成功率，求见还必须满足皇帝此刻在殿内。求见成功后经李公公通传进入皇帝日间单人交互页；闲聊会透露皇帝行程和心情，打点银两扣银两并提高好感。本条修订第 22、30、31 条。
33. v0.5.4 - 皇帝日程真值与李公公情报分层：皇帝本旬各时辰行程改为保存在 `progress.emperorInteraction.schedule`，当前心情保存在 `progress.emperorInteraction.mood`，公共偶遇、养心殿求见和李公公闲聊都读取该状态，不再在 UI 或求见时临时生成行程；侍寝结算更新皇帝心情时同步更新日间皇帝状态。李公公闲聊按好感分层透露：低好感只说是否在殿，中等好感透露当前去向和大致心情，高好感透露接下来数个时辰的行程与更明确心情。存档 schema 提升到 `6`，旧 `emperorInteraction` 缺少日程 / 心情字段时按开发期规则清档。本条修订第 31、32 条。
34. v0.5.4 - 外景侧栏回宫语义统一：玩家处于地图子场景 / 后宫外景时，左侧栏第二个入口与大地图一致显示为 `回宫` 并直接返回寝殿；删除此前临时竖排 `回宫` 按钮。返回地图主视角改由场景内 `离开` 入口承担，后宫总览也补充 `离开` 按钮以保持子场景一致性。本条修订第 30 条。

## v0.5.3 - 后续系统迭代（已归档）

> 已归档。v0.5.3 主要收口地点入口与泛用地点交互、全屏场景容器、随机事件系统第一版、开局资源 / 家世点数规则、作品制作入口与售价公式，以及晋升清晨通报和圣旨演出。版本后段重点修复晋升通报时序与宽屏圣旨排版，使清晨通报只能在黑屏后出现，圣旨演出改为内侍送旨、素材题签、日期正文和高度约束的卷轴展开。

1. v0.5.3 - 晋升清晨通报修订：玩家跨月自然晋升仍由声望与 `rankRuntime` 推进实际位分和住处，但新增最高优先级“晋封旨意”太监通报，排在普通月初 / 旬报、侍寝通报和养心殿裁断前展示；寝殿与地图场景均使用传旨内侍立绘，通报队列确认后再顺序播放普通月初通报。
2. v0.5.3 - 位分表字段清理：检查冷宫 NPC 与位分图标字段。冷宫妃嫔已由 `fixed_consort_roster.csv` 维护，路线冷宫老宫人仍属于主线剧情说话人；确认 `rank_prestige_table.csv` 的 `iconPath` 没有真实资产目录和 UI 消费后移除该字段、对应类型与无效常量，位分表只保留门槛和颜色标识。
3. v0.5.3 - 地点入口与常规行动收口：大地图热点卡统一只保留 `进入此处 / 留在地图`，地点内互动改由 `ChamberMainView` 下的地点子场景承载；新增 `GenericMapLocationView` 和 `mapLocationActions.ts`，为御花园、正阳门、重华宫、冷宫、养心殿等空地点提供常规行动和地点内特殊入口；宫门迁入 `GongmenView`，杜娘 / 阿翎、交易和宫门妃嫔偶遇不再由地图页假场景处理。
4. v0.5.3 - 地点入场对白修订：泛用地点的入场对白改为本次进入只播放一次，地点内行动造成数值或时辰变化时不再重新触发“行至某地前”的入场对白，保证养心殿抄读等常规行动结果对白能正常显示 - 见第 3 条。
5. v0.5.3 - 泛用地点行动结果对白层级修订：御花园、正阳门、重华宫、冷宫、养心殿等泛用地点的行动结果对白不再嵌套在地点操作卡片内部，而是作为地点场景的全局对白层渲染，避免被小卡片尺寸限制导致对话框过小或正文不可见 - 见第 3、4 条。
6. v0.5.3 - 场景级容器全屏修订：启动、路线选择、属性分配、开场、地图和寝殿的场景级 frame 统一填满浏览器 viewport，移除外层 16:9 圆角舞台、边框、投影和留边；圆角只保留给内部面板、弹窗和按钮等 UI 组件。
7. v0.5.3 - 地图热点比例修订：场景全屏后，竖排 `map-main__hotspot` 宽度不再随超宽 viewport 继续横向膨胀，而是按 16:9 设计舞台换算宽度封顶，恢复地点标志的正常竖牌比例 - 见第 6 条。
8. v0.5.3 - 随机事件系统第一版：新增 `src/game/random-events/csv/random_events.csv` 单表与说明文档，用 `event / line / option` 行维护事件池、前置、剧情、选项、效果和解锁；新增 `randomEventCatalog` 校验重复 ID、非法分支、非法前置 / 解锁和非法效果 JSON，新增纯 `randomEventRuntime` 支持按 `poolId`、前置、一次性 / 可重复和权重抽取事件，推进剧情行、选择选项、完成计数并应用玩家 / 目标 / 背包局部效果。本轮不接入 UI、store、存档或现有事件入口。
9. v0.5.3 - 开局资源下调：玩家初始背包清空，`inventory_items.csv` 不再有物品进入 `initial` 池；初始可分配属性点整体下调 8 点，普通家世预算改为 `40..48`，兰因絮果 `40..43`，浮生如梦 `40..46`，影落掖庭固定 `46`，和亲路线仍锁定为 `0`。
10. v0.5.3 - 初始属性点与家世声望规则重构：新增 `family_initial_traits.csv`，家世拆成品级、文武、嫡庶、商贾、国公、罪臣、和亲等词条；初始可分配点数改为 `initial_attribute_base_points(20) + 路线 pointModifier + 家世词条 pointModifier`，不再随机，不再用 `resolveFamilyBasePoints` 中文分支；每月家世声望也改为读取同一词条表的 `monthlyOfficePrestige + monthlyBackgroundPrestige`。本条修订第 9 条中“40..48 家世预算”的旧口径。
11. v0.5.3 - 作品制作入口修订：宫内字画 / 刺绣 / 调香面板改为 `才思泉涌` 与 `练习` 两个行动；`练习` 只做普通加点，`才思泉涌` 消耗同一次寝殿行动并从 `craft_works.csv` 中满足 `unlockStatKey / unlockStatMin` 的作品池随机引出新作品，进行中作品点击自身即可继续推进。作品池扩充为更偏古代题名的绣品、画作和香方，完成品仍进入背包礼物分类。
12. v0.5.3 - 制作剧情反馈修订：`才思泉涌` 不再只复用普通寝殿行动总结，而是按字画 / 刺绣 / 调香分别读取 `map_chamber_dialogues.csv` 中的独立小剧情，并在剧情中明确展示随机到的作品名；补齐 `调制香薰` 专属寝殿行动剧情，避免调香行动只剩 `容貌 +1` toast 反馈 - 见第 11 条。
13. v0.5.3 - 作品品质与售价修订：收紧作品成色判定，`工稳 / 精妙` 阈值从 `50 / 75` 调整为 `65 / 90`，质量评分公式降低基础值和属性权重，使高品质作品更难获得；品质售价倍率从 `粗成 0.75 / 工稳 1 / 精妙 1.35` 调整为 `粗成 0.9 / 工稳 1 / 精妙 1.12`，售价公式把难度从 `difficulty / 100` 改为 `difficulty / 1500` 的归一化小幅修正，避免制作难度评级直接把价格翻倍 - 见第 11 条。
14. v0.5.3 - 晋封圣旨演出与声望 debug：开发期控制台新增 `palaceDebug.addPrestige(数量)`，与加银两命令一样通过 store 正式动作同步 `state.prestige` / `hiddenStats.prestige` 并触发数值反馈；玩家晋升通报改为专用圣旨展开演出，使用圣旨底图、卷轴展开动画和竖版旨意文本，动画结束后点击圣旨收起并继续后续清晨通报队列 - 见第 1 条。
15. v0.5.3 - 清晨通报黑屏时序与圣旨演出修订：所有清晨结算通报必须在黑屏淡出结束后显示，睡觉与侍寝结算都会先复位到寝殿清晨；晋封通报改为“内侍先在寝殿送旨 -> 点击后遮罩展开圣旨”，卷轴两侧柄部从中间拉开并保留，圣旨正文按竖线分列，“圣旨”统一字号，晋封后的位分单独成列放大显示 - 见第 1、14 条。
16. v0.5.3 - 晋封圣旨素材与正文修订：圣旨题签不再用文字绘制，改用 `material-202606231107-018.png` 与 `material-202606231107-019.png` 两张素材叠放在右侧圆形题签区域；圣旨正文新增宫历日期列，正文区域避开题签圆纹并继续按底图竖线分列；两侧卷轴柄高度调整为长于中间画幅，展开后与画幅边缘衔接 - 见第 14、15 条。
17. v0.5.3 - 晋封圣旨宽屏裁剪修订：圣旨舞台尺寸改为同时受视口宽度和高度约束，避免宽屏低高视口下横幅被外层裁切；正文列改为从右侧题签左边固定起排，降低列间距并增加上下安全边距，长句按标点拆列，防止“钦此”等末尾列被推入裁剪区域；“圣”字素材放大并增加“圣 / 旨”间距 - 见第 16 条。

## v0.5.2 - 后续系统迭代（已归档）

> 已归档。v0.5.2 主要完成数值系统配置化与公式页拆分，扩展宫斗 / 侍寝 / 随机妃嫔等可调参数，并新增绣花、字画、调香作品制作闭环。版本后段收口进出场景时间规则、宫门工具 NPC 交互和文档维护边界，使普通进出场景不再消耗时辰或体力，真实行动才推进时间。

1. 归档 `v0.5.1 - 调查与定罪闭环`，后续系统修改进入 `v0.5.2`。
2. 调整体力消耗规则：大地图进入地点、地点快捷面板与后宫殿位进入妃嫔会面不再扣体力，只保留时辰推进；体力为 `0` 时仍不能开始妃嫔拜访，地点内听曲、问诊、祭拜等具体行动继续按各自规则消耗或恢复体力。
3. 数值系统配置化：新增 `src/game/numerics/csv/` 与 `numericCatalog`，将属性字段、全局范围与倍率、路线初始、寝殿行动、月用度、位分 / 宠爱分层、库存物品和固定妃嫔种子数值迁入 CSV；新增 `saveGameConfig.ts` 集中维护存档 schema、storage key、必需字段和默认进度块。旧导出名保留为兼容层，但核心值改由 catalog 提供。
4. v0.5.2 - 宫门杜娘闲谈修订：当前版本不接 AI 正文，杜娘闲谈点击后只使用本地 CSV 轮换文本，不再发起后续本地请求覆盖刚显示的闲谈回应 - 见第 3 条。
5. v0.5.2 - 数值拆表扩展：继续将宫斗严重度、检定修正、NPC 宫斗阈值、调查推进、银两干预、养心殿裁断倍率，夜晚侍寝独寝率、宠爱权重、兴致收益、互动加成、第三方影响，以及随机补足妃嫔模板、开局目标人数和属性浮动迁入 numerics CSV；runtime 只保留公式、随机流程和状态落地 - 见第 3 条。
6. v0.5.2 - 公式配置修订：撤回上一条中“公式碎片写入 CSV”的做法；主动宫斗检定、嫌疑人动机、初始定案率、银两干预和侍寝互动加成改为完整公式字符串，维护在独立 `*FormulaPage.ts` 并由受限公式解析器求值；CSV 只保留表格型数据和结果档位 - 见第 5 条。
7. v0.5.2 - 旧听审流程删除：移除旧 `resolveYangxinHearing`、`YangxinHearingPanelView`、`yangxinHearing*` 案件字段和对应测试；宫斗定案只走“调查到 100 -> pending_verdict -> 养心殿传唤对话裁断”流程 - 见第 6 条。
8. v0.5.2 - 公式目录修订：公式页移入独立 `src/game/numerics/formula-pages/`，该目录只放可编辑公式和注释；解析器、调用包装与测试继续留在 `src/game/numerics/formulas/`，避免公式页和工程代码混杂 - 见第 6 条。
9. v0.5.2 - 侍寝声望规则修订：玩家被传召侍寝固定获得声望 `+10`；其他妃嫔侍寝后若因好感为玩家美言，除宠爱变化外也会增加玩家声望，且位格越高增量越高。相关参数继续维护在 `nightly_runtime_rules.csv`，位格换算复用妃嫔位分权重 - 见第 5 条。
10. 新增绣花 / 字画 / 调香作品制作闭环：点击寝殿对应行动会进入该类别制作面板，可添加作品、选择进行中作品推进或只练习技艺；进度受主技艺、辅助属性、作品难度和完整公式页影响。完成品作为 `gift` 道具进入背包，可送礼或按难度、能力和成色折算售价变卖；作品题材和基础数值维护在 `craft_works.csv`，公式维护在 `craftWorkFormulaPage.ts`，存档 schema 提升到 `4` 并保存 `progress.craftWorks`。
11. v0.5.2 - 裁断选择规则修订：移除旧裁断流程残留的 `argue / plead / accept` 选择 ID；当前养心殿裁断只使用玩家本体和旁听两组 CSV 选项，NPC-only 静默裁断默认走 `state-facts` 中性规则，避免代码默认值与 `yangxin_verdict_choice_rules.csv` 不一致 - 见第 7 条。
12. v0.5.2 - 寝殿行动与作品入口修订：作品面板移除“只练习技艺”和“已完成库存”，完成品只进入背包；绣花 / 字画 / 调香必须选择作品推进才结算行动。寝殿行动数值改为调香 `体力 -3 / 容貌 +1`、诵读 `体力 -2 / 诗词 +1 / 政治 +1`、刺绣 `体力 -3 / 刺绣 +1`、奏乐 `体力 -2 / 气质 +1 / 乐理 +1`、作画 `体力 -3 / 丹青 +1`、平安脉 `健康 +5` 且每旬一次、殿内小憩 `体力 +3 / 压力 -1` - 见第 10 条。
13. v0.5.2 - 作品进度公式修订：移除 `craft_works.csv` 的 `averageActionCount` 字段，制作进度不再由固定平均次数决定；单次进度改为只读取玩家主能力、辅助能力、作品难度和稳定浮动，制作面板的预计剩余次数也改用同一公式估算 - 见第 10 条。
14. v0.5.2 - 进出场景时间规则修订：进出场景只切换玩家所在位置，不再推进时辰；大地图进入普通地点、地点快捷面板和影落掖庭地图事件入口均不调用 `advanceTime`，只有地点内听曲、问诊、拜访、礼佛等真实行动才消耗时间并触发深夜 / 体力归零睡觉流程 - 见第 2 条。
15. 文档维护收口：新增 `docs/README.md` 和 `docs/archive/README.md`，明确当前维护文档、归档文档和写入规则；旧 V1 规则稿、自然语言说明稿、旧 B 版宫斗稿、旧 AI / Foundation 接口说明和世界记忆 proposal 移入 `docs/archive/`，后续不再同步维护。
16. v0.5.2 - 文档归档规则修订：`codex-dialogue-handoff.md` 和 `system-hard-rules-integrated.md` 因内容过度膨胀、重复记录大量改动，移入 `docs/archive/retired-overviews/` 并停止维护；新对话入口改为读取 `docs/README.md`、最新 changelog 和任务相关专项文档，玩法规则不再维护单一总规则文档 - 见第 15 条。
17. v0.5.2 - 宫门工具 NPC 交互修订：杜娘入场对白读完后不再自动退出人物面板，买卖 / 闲谈入口保持可用；退出改为显式点击“返回宫门”，避免线性对白推进吞掉工具 NPC 交互。
18. v0.5.2 - 外景行动与宫门工具 NPC 修订：正阳门“等待下朝”改为真实地点行动并推进一格时辰；杜娘等宫门工具 NPC 的买卖 / 闲谈操作栏改为入场对白读完后才出现，并移除工具栏解释性说明文本；阿翎“叙旧”改读 `npc_tools_dialogues.csv` 正式对白，不再使用开发占位文案 - 见第 14、17 条。

## v0.5.1 - 调查与定罪闭环（已归档）

> 已归档。v0.5.1 将宫斗调查从单一获罪率推进为“嫌疑人定案率 -> 待裁断 -> 养心殿求情 / 裁断 -> 归档”的闭环，并补齐妃嫔会面次数、送客收束和皇帝日间互动。版本后段完成剧情 CSV 配置化与唯一源收口，断开当前玩法中的 AI 正文链路并移除 `nextActionLabel` 等第二剧情源字段。

1. 归档 `v0.5.0 - 宫斗与调查闭环`，后续调查与定罪闭环修改进入 `v0.5.1`。
2. 宫斗案件从“单一案件获罪率”升级为“案件下最多三名嫌疑人各自定案率”：案件保存 `suspects`、`convictedSuspectId`、`archivedXunKey` 与 `resolutionSummary`；旧 `convictionRate` 只作为最高嫌疑人的派生展示值。
3. 暴露案件会按实际发起者、被嫁祸者、玩家牵连状态和妃嫔动机生成嫌疑人列表；被嫁祸者强制进入嫌疑名单且初始定案率至少 `70`。
4. 调查推进改为每旬推进所有嫌疑人：轻 / 中 / 重分别 `+8 / +14 / +20`，被嫁祸者额外 `+5`；任一嫌疑人达到 `100` 立即定罪，三旬无人到 `100` 则归档为疑案。
5. “宫斗事务”新增动态调查页：有调查中或已归档案件时才显示“调查”入口，调查中案件展示摘要、受害者、方式、已查旬数、嫌疑人、定案率和嫌疑原因；归档案件展示定罪者或疑案结论与最终嫌疑分布。
6. 玩家可在调查页对任意嫌疑人花 `20` 银两执行定案率 `-5` 或 `+5`；银两不足或案件已结时不能干预，推高到 `100` 会立即触发定罪并进入归档。
7. 审判 / 求情第一版继续复用养心殿裁断；若玩家被定罪则沿用玩家声望 / 宠爱 / 压力惩罚，若 NPC 被定罪则惩罚对应妃嫔声望并继续接冷宫规则。
8. 开发期存档校验收紧：宫斗案件缺少 `suspects` 数组视为结构不兼容，按当前开发期规则直接清除旧存档，不做旧结构 fallback。
9. v0.5.1 - 裁断流程修订：嫌疑人定案率达到 `100` 不再立即定罪归档，而是写入 `status='pending_verdict'` 与 `pendingVerdictSuspectId`；三旬无人达到 `100` 仍直接归档疑案 - 见第 4 条。
10. v0.5.1 - 调查页干预修订：花 `20` 银两推高嫌疑到 `100` 只进入“待裁断”，不会立刻扣声望、宠爱或压力；调查页新增“待裁断案件”展示 - 见第 6 条。
11. 新增养心殿裁断对话事件：玩家相关待裁断案会在下一旬清晨优先触发内侍传旨，强制切入养心殿对话舞台；相关人发言后玩家可选“据理力争 / 委婉求情 / 沉默认罚”，求情只减罚不翻案。
12. 裁断完成后才写入 `convictedSuspectId`、`verdictSummary`、`penaltyApplied` 并应用声望 / 宠爱 / 压力处罚；玩家被定罪惩罚玩家，NPC 被定罪惩罚对应妃嫔，NPC-only 案件不打断玩家，按内廷裁断静默结案。
13. v0.5.1 - 养心殿入口修订：旧 `YangxinHearingPanelView` 不再作为玩家主动工具面板入口；地图养心殿不再提供“养心殿裁断”快捷按钮，裁断只由待裁断事件传唤触发 - 见第 7 条。
14. v0.5.1 - 存档结构修订：`SaveGameV1` schema 提升到 `2`，`cases.pendingYangxinVerdict` 成为当前结构字段；旧 schema 或缺字段存档按开发期规则清除 - 见第 8 条。
15. v0.5.1 - 养心殿场景修订：裁断传唤不再使用独立假场景或黑屏承载，而是把 `activeMapLocation` 切到真实 `养心殿`，使用养心殿内景和现有对话舞台展示内侍、皇帝、娇娇、玩家或妃嫔立绘；裁断结束后仍停留在养心殿，由玩家自行离开 - 见第 11 条。
16. v0.5.1 - 玩家本人裁断称呼修订：玩家作为定罪候选人或被嫁祸者时，养心殿对白以“你 / 娘娘”称呼，不再在对话正文中显示玩家本名 - 见第 11 条。
17. v0.5.1 - 养心殿背景拆分修订：普通进入养心殿恢复地点外景和入场剧情；裁断事件单独使用 `yangxin_verdict_daytime.png`，不再复用侍寝图或污染普通地点进入 - 见第 15 条。
18. v0.5.1 - 玩家嫌疑人裁断选项修订：玩家本人是定罪候选人时，裁断选择显示“为己申辩 / 低头请罪 / 沉默领罪”；旁听或替他人求情时仍使用原求情口径 - 见第 11 条。
19. v0.5.1 - 裁断选项身份分流修订：玩家本人是定罪候选人时改为五项“据证自辩 / 陈明疑点 / 伏身求宽 / 攀指旁人 / 沉默领罪”；玩家不是嫌疑人时改为五项“请皇上严断 / 只陈案情 / 指出疑点 / 代为求情 / 沉默旁听”，并分别调整处罚倍率与关系影响 - 见第 18 条。
20. v0.5.1 - 养心殿传唤场景修订：内侍传旨阶段保留在玩家寝宫，玩家确认“前往养心殿”后才切换到 `activeMapLocation='养心殿'` 与裁断专用背景，避免传唤还未发生就进入裁断场景 - 见第 15 条。
21. 新增玩家与妃嫔互动回合限制：`consortInteractionMap` 记录每旬每名妃嫔的互动次数，玩家主动会面、公共地点关系判定每旬每人最多 `3` 次；妃嫔会面内单个话题最多继续 `2` 句。最后一次互动后会演出宫人送客并退出会面，同旬再次拜访会在殿位处由宫人婉拒，不消耗体力和时间。NPC 拜访玩家仍按行动表 `triggeredVisitIds` 每条拜访只触发一次；旧存档若已有互动记录但缺少 `actionCountThisXun`，按开发期规则直接清档。
22. 新增皇帝日间行为与互动机制：皇帝按时段在正阳门、养心殿、御花园、建章宫活动；正阳门清晨可等待下朝，养心殿上午到傍晚可求见，御花园 / 建章宫中午到傍晚可概率偶遇并进入完整皇帝互动页，夜晚 / 深夜养心殿由内侍劝归。
23. 皇帝交互页新增一轮主行动和一次附加话题：主行动包括研墨、按摩、关心、抚琴、闲聊、撒娇、入怀、对弈；附加话题在送礼、美言、诉苦中三选一。送礼消耗真实背包食物 / 绣品 / 字画，美言使目标妃嫔声望 `+5`，诉苦使目标妃嫔声望 `-5`。
24. v0.5.1 - 存档结构修订：`SaveGameV1` schema 提升到 `3`，`progress.emperorInteraction` 保存皇帝互动触发记录；旧 schema 或缺少该字段的存档按开发期规则清除 - 见第 14 条。
25. v0.5.1 - 会面收束演出修订：妃嫔会面最后一次互动必须先完整播放本次互动结果，再进入送客收束；公共地点偶遇使用“让开宫道 / 今日偶遇到此为止”的外景收束，不复用殿内送客。对白未收起时妃嫔固定操作禁用，避免入场对白、行动结果和送客互相抢流程。皇帝公共偶遇完成主行动后也先播放行动结果，再播放“恭送圣驾”收束，不直接黑屏离开 - 见第 21、22 条。
26. 新增剧情正文 CSV 配置层：`src/game/narrative/csv/` 按开场、地图寝殿、路线主线、地点偶遇、关系会面、皇帝 / 养心殿、宫斗裁断、工具 NPC 拆分剧情表；`narrativeCatalog` 通过 Vite `?raw` 读取并校验重复 ID、空正文和立绘位置。开场、用度说明、地图引导、寝殿 / 地点行动、妃嫔 / 太后本地剧情、宫门 NPC、皇帝日间互动、侍寝演出、养心殿裁断固定对白已改为引用剧情 ID，状态流转、按钮、数值公式仍留在代码中。
27. 补充 `src/game/narrative/csv/README.md`，解释剧情 CSV 各文件分工、统一表头含义、变量 / 分页 / 转义写法、代码读取方式和 loader 校验规则，并单独标出文案策划最常编辑的说话人、立绘、正文、场景提示等字段。
28. v0.5.1 - 剧情 CSV 表头修订：移除 `nextActionLabel` 列；剧情 CSV 只维护正文和演出元数据，不再混入下一步按钮文案 - 见第 26、27 条。
29. v0.5.1 - 开场剧情配置化修订：`openingDialogueRuntime` 不再只读取 CSV 的 `text`，改为从 CSV entry 组装 `speakerIdentity`、`speakerName`、`portraitKey`、`portraitPlacement`、`narrationName` 与 `text`；开场响应和服务端 opening-dialogue 契约同步移除 `nextActionLabel`，用度解释返回分支改用显式流程状态判断 - 见第 26、28 条。
30. v0.5.1 - 剧情 CSV 消费修订：修正首轮配置化只迁正文的问题；寝殿主舞台、地图引导、妃嫔 / 太后本地剧情、地点偶遇本地剧情、送客收束、宫门工具 NPC、皇帝互动、侍寝、养心殿裁断和影落掖庭主线均改为优先读取完整 CSV entry，由 CSV 提供说话人、立绘 key、旁白名、正文和场景提示。业务代码移除直接 `renderNarrativeText` / `renderNarrativeField` 调用，只在底层 helper 和测试中保留文本读取能力 - 见第 26、27、29 条。
31. v0.5.1 - 剧情 CSV 消费层修订：新增 `narrativeDialogueAdapter` 作为运行期统一消费入口，开场响应、寝殿主舞台、地图引导、宫门工具 NPC、影落掖庭主线、地点本地剧情、送客收束和养心殿裁断均改为通过 adapter 将 CSV entry 转为当前 UI 所需结构，避免业务代码继续逐项拆 `entry.speakerName`、`entry.text`、`entry.portraitKey` - 见第 30 条。
32. v0.5.1 - 剧情 CSV 唯一源修订：当前玩法链路断开 AI 生成剧情正文，开场、妃嫔、太后、地点偶遇、地点环境文本和宫门工具 NPC 均改为 `request*LocalDialogue` / `request*AmbientLocal` 本地入口；删除剧情 turn、组件 prop、前后端契约和测试中的下一步按钮数据字段，推进按钮文案不再随剧情数据传递。`fallbackToneTag` 统一改为 `localToneTag`，关系判定来源统一为 `source='local'`，新增 `narrativeContract.test.ts` 扫描防止当前玩法代码重新引入第二剧情源 - 见第 26、28、31 条。

## v0.5.0 - 宫斗与调查闭环（已归档）

> 已归档。后续调查与定罪相关修改进入 `v0.5.1`。

1. 归档 `v0.4.0 - NPC 行动与外景视图收口`，后续系统修改进入 `v0.5.0`。
2. 玩家下毒接入第一版投放 QTE 表现：宫斗事务选择“下毒”后，点击完成会先进入杯盏时机条；药理影响成功区域宽度，心计与压力影响指针速度，投放成功后才登记原有当旬待结算宫斗案件。
3. v0.5.0 - 调高初始属性下的下毒 QTE 难度：降低基础成功区间、提高初始指针速度，高药理 / 高心计仍能明显降低难度 - 见第 2 条。
4. NPC 宫斗目标池接入玩家：低关系高动机 NPC 可直接对玩家发起宫斗，也可在攻击其他妃嫔时把玩家列为嫁祸对象；玩家被害或被嫁祸并进入调查时，会标记为需要养心殿裁断。
5. 后宫初始存活妃嫔扩充到 `12` 人：生成 roster 会按路线固定妃嫔数量补足随机妃嫔模板，并新增程雪砚、阿史那明珠、闻人照月、周怜星等人设模板；连翘继续作为妙音堂 NPC，不进入妃嫔名单。
6. 掖庭院新增“掖庭掌事 · 月姑姑”毒药交易入口，可购买陨颜丹、麝香、鹤顶红；毒药从杜娘普通货单中拆出，宫斗事务下毒必须先持有对应毒药，QTE 投放成功并登记案件时消耗 `1` 份，QTE 失败不消耗。
7. v0.5.0 - 玩家下毒 QTE 不再只是表现层：下毒入口增加背包校验、毒药持有数量展示和重复登记保护，避免无毒药或重复点击完成时绕过资源消耗 - 见第 2 条。
8. v0.5.0 - 修复玩家主动宫斗扣福德 toast 延迟：`startPalaceStrifeCase` 在登记案件并扣福德后同步触发 `chamber-action` 数值反馈，避免下毒消耗道具先释放 toast、福德变化滞留到下一事件 - 见第 2 条。
9. v0.5.0 - 收口福德数值单位：属性分配阶段仍按 `1` 加点折算 `10` 福德显示，确认进入剧情时写入运行时福德真值；玩家面板、toast、侍寝怀孕概率和主动宫斗扣除均直接使用运行时福德值，主动造谣 / 下毒按文案实际值扣 `5 / 10` - 见第 8 条。
10. v0.5.0 - 收口全属性加点单位与运行时真值：确认进入剧情时统一将健康 / 心计 / 容貌 / 气质、福德、副属性从属性分配点数折算为运行时真值；正式游戏内寝殿技能条、玩家面板、toast、侍寝、宫斗、QTE、太医院剧情、熬夜和月度用度结算均读取运行时值，避免二次换算或把加点点数当真值 - 见第 9 条。
11. v0.5.0 - 修复已确认属性存档回溯到创建面板后属性显示被再次乘倍率：回溯时若 `attributeStatsFinalized=true` 且开场未完成，直接恢复到开场对白；创建面板异常接收到已确认属性时只展示运行时真值并禁用加减 - 见第 10 条。
12. 新增开发期浏览器控制台 debug 指令 `palaceDebug.addSilver(数量)`：调用后通过 store 正式动作增加玩家银两，同步 `state.silver` 与 `hiddenStats.silver`，并触发对应数值反馈；该指令只作为开发调试入口，不是正式经济来源。
13. v0.5.0 - 修复宫斗事务对象列表仍按旧 6 人面板截断的问题：玩家主动宫斗的目标、嫁祸对象和可合谋妃嫔改为从当前存活非冷宫 roster 动态完整读取，新扩充到 12 人的妃嫔也可被选择 - 见第 5 条。

## v0.4.0 - NPC 行动与外景视图收口（已归档）

> 覆盖原 `2026-06-03` 与 `2026-06-04` changelog 内容；未发现 `2026-06-05` changelog 条目。

1. 修复非寝宫场景左侧 UI 与寝宫状态耦合的问题：大地图常驻“嫔妃 / 查看 / 纪事 / 情缘”改为地图覆盖面板，不再先把玩家传回寝宫；外景面板中点击“外出”仍返回地图主视角。
2. 调整数值 toast 口径：NPC-NPC 交流导致的妃嫔压力变化仅静默写入状态与关系矩阵，不再作为玩家清晨 / 结算 toast 展示；妃嫔声望变化仍可进入 toast。
3. 将本地个人测试目录 `__test__/` 与开发截图输出 `output/` 加入 `.gitignore`，避免同步每个开发者的临时测试信息。
4. 新增妃嫔旬级行动表与 NPC-NPC 关系矩阵：每旬为存活且非冷宫妃嫔生成留宫、公共外出、拜访玩家、拜访妃嫔、社交筹谋或低频敌意筹谋。
5. NPC 公共外出地点改为从地图热点配置派生，不再只覆盖妙音堂、御膳房、太医院、宝华殿、华清池；御花园、正阳门、重华宫、宫门、建章宫、冷宫、养心殿等空地点也可出现本旬妃嫔动向。
6. 公共地点偶遇优先读取本旬 NPC 行动表；无安排时不再从全体 live 妃嫔里随机抓人，避免“所有地点都能硬刷同一批 NPC”的假随机。
7. NPC 拜访其他妃嫔会让被拜访者本旬留在殿内接待；玩家进入被拜访者宫殿时可看到两名妃嫔同场与互动摘要。
8. 调整 NPC 遇见方式：NPC 拜访玩家会在寝殿空闲时自动打断并给出选择；公共地点和空地图点的 NPC 改为可点击交互，点击后用地点入场叙述和行动摘要进入妃嫔日常对话。
9. 修复外出 NPC 目的地可见性：地图地点弹窗只显示本旬在该地的妃嫔动向，不新增“前去见某妃”直达选项；玩家进入地点后再看到可交互妃嫔。`resolved` 只表示本旬已交谈，不再把 NPC 从目的地移除。
10. 后宫宫殿中，若某妃嫔本旬在自己殿中接待来访者，会在殿位和拜访入口标记为“会客中”。
11. NPC 动向调试只输出到浏览器控制台：进入寝殿或地图时用 `[npc-activity]` 和 `console.table` 展示本旬妃嫔行动，不新增游戏内调试层。
12. 收口 NPC 位置真值：公共外出或拜访他人的妃嫔不再能在自己寝宫被拜访；NPC 拜访玩家每旬最多 1 人且概率降低，NPC 拜访其他妃嫔时同一目标每旬最多接待 1 人。
13. 修复后宫作为地图地点时的返回语义：从地图进入后宫后，侧栏“外出”返回地图主视角，独立“回宫”才直接回寝殿，不再先经过玩家住所。
14. 修复 NPC 动向 debug 与后宫场景不一致的问题：未收束的 `visit-consort` 目标妃嫔按“在自己寝宫会客”显示，并从公共地点可见名单中排除；旧居类住址会归一到对应宫殿主殿。
15. 修复玩家结束 NPC 会客后的位置收束：`visit-consort` 被玩家会客结束后标记为已触发，来访妃嫔回到自己的寝宫，被拜访者不再显示“会客中”。
16. 后宫拜访妃嫔接入统一过夜链路：深夜拜访结束后由娇娇提醒、黑屏过夜，再进入清晨通报；熬夜惩罚改为在清晨通报中由娇娇说明并扣除，压力 +2、健康 -10 显示值、气质 -10 显示值。
17. NPC 宫斗改为每旬全局一次低概率敌意筹谋入口，每旬最多生成一条 NPC 宫斗案；宫斗候选来自 `hostile-plot` 行动或高动机妃嫔。
18. 朝堂事务与宫斗事务文案拆清：宫斗事务会登记真实案件；朝堂事务当前只暂存草案，不改数值、不进结算。
19. SaveGameV1 可选扩展 `progress.npcActivity` 与 `relations.npcRelationMatrix`，旧存档缺失时按空状态初始化，不提升 schema version。
20. 修复合并冲突：合并 `ChamberMainView` 中“吩咐娇娇”寝殿面板与 NPC 地点偶遇 / 拜访渲染链路，保留外景 NPC 可点击交互、NPC 拜访玩家、地点子场景和寝殿对白规则。
21. 修复合并后的测试口径：竖排寝殿行动按钮按无障碍按钮名断言，不再按拆分文本节点查找。
22. 修复后宫外景与寝殿状态耦合：从后宫打开“嫔妃 / 查看 / 纪事 / 情缘”等左侧工具面板后，关闭会回到后宫总览，不再落到空白外景或玩家寝殿。
23. 修复其他场景点击左侧“外出”的中转问题：处于后宫等外景工具面板时，“外出”直接返回地图主视角，不再以玩家寝宫作为中转。
24. 删除地图后宫热点中多余的“后宫总览”快捷选项，后宫入口统一使用默认“进入此处”进入外景。
25. v0.4.0 - 外景回地图的清理时机延后到退出动画完成，避免视觉上短暂闪回玩家寝宫 - 见第 23 条。
26. 调整影落掖庭初始住处：玩家正式寝殿从掖庭院迁至储秀宫西偏殿；掖庭院改为普通地图地点，开局定下用度后由娇娇带玩家经过后宫，直接触发陈婉宁初见，再回新寝殿。
27. v0.4.0 - 开发期存档策略改为不做旧结构迁移；`SaveGameV1` 结构不兼容时直接删除旧 envelope，移除旧 raw store、旧住处和旧字段 fallback - 见第 19 条。
28. v0.4.0 - 影落掖庭开局不再跳过娇娇地图引导；定下用度后先展示整张地图说明，完成后再前往后宫触发陈婉宁初见并回新寝殿 - 见第 26 条。
29. 后宫宫殿总览中，玩家自己的殿位新增“回到当前寝殿”入口，可从后宫殿位布局直接回家。
30. 全局屏蔽普通 UI 文本、图片、SVG、canvas、video 的选中和拖拽提取；输入框与可编辑区域仍保留文本选择能力。
31. v0.4.0 - 大地图移除独立 `椒房殿` 热点，原错误点位改回 `掖庭院`；回寝殿统一通过侧栏 `回宫` 或后宫玩家殿位入口 - 见第 26 条。
32. v0.4.0 - 大地图地点弹窗不再展示本旬 NPC 名字、摘要或动向；NPC 只在进入地点后的场景中作为可交互对象出现 - 见第 9 条。

## 2026-06-02

- 修复剧情 / 对话文本显示时背景 UI 仍可被点击的问题；共享对话舞台新增透明交互锁层，寝殿与地图主入口补充运行时点击保护。
- 修复共享对话进入选项态后，正文仍可点击并显示鼠标焦点框的问题；选项态下正文只保留可读，不再参与推进。
- 调整寝殿夜晚推进顺序：普通行动先展示本次行动结果，再展示夜晚侍寝通报或侍寝事件。
- 拆分数值 toast：每个数值差异独立生成一条 toast，最大可见数量调整为 10。
- 调整数值 toast 触发时机：数值变化先入队，等对应事件对白、夜晚通报或旬月通报实际出现后再显示。
- 调整侍寝反馈：他人侍寝导致玩家宠爱变化时，只保留状态变更与通报，不再弹宠爱 toast。
- 移除体力数值 toast，体力变化只通过常驻状态栏体现。
- 增加清晨通报去重保护，避免过夜推进过程中同一旬清晨通报被重复生成或重复展示。
- 移除宠爱相关 toast，玩家宠爱与妃嫔宠爱变化不再弹出数值提示；声望相关变化继续进入 toast。
- 寝殿主界面的“调整用度”新增第四个说明选项，由娇娇解释三档用度后返回选择，不写入策略状态。
- 修复玩家与妃嫔声望扣减不能低于 0 的问题；声望统一按 `[-2000, 5000]` 限制，为负声望与冷宫规则保留真实入口。
- 修复新建路线 / 新存档时旧局时间、旬月通报、宫斗案件、侍寝记录、交易记录等持久状态未被覆盖的问题；路线选择现在从初始状态重建一局。
- 启动页“开始”接入二级确认；确认后清空旧存档、重建初始状态并创建新存档。
- 启动页“回溯”接入单槽读档，可读取上一次 `SaveGameV1` 并恢复到对应游戏阶段；无存档时显示提示。
- 梳理启动菜单“前尘 / 回溯”现状：`回溯` 已接入读档，`前尘` 仍为按钮预留文案，未接入独立玩法。
- 新增 `docs/save-system-maintenance.md`，统一记录存档 key、SaveGameV1 envelope、新局 / 回溯入口和字段维护要求。
- 更新相关测试、交接文档、系统拆解文档与 `codex-workdocs` 记录。
- 修复妙音堂听曲数值 toast 延迟到旬月通报才显示的问题：听曲先结算压力变化，再推进时间，并在妙音堂行动结果出现时触发本次行动 toast。
- 接入系统宫宴第一版闭环：宫宴报名开启会生成独立事件通报，妙音堂曲谱报名写入 `PalaceBanquetProgressState`，3 月上旬傍晚自动结算宫宴表现和声望变化。
- 新增宫宴季调度与结算 runtime，曲谱品质、乐理、听曲次数和连翘关系会影响宫宴完成度；宫宴结果写入存档并防止同届重复触发。
- 修复时间通报只绑定寝殿主界面的问题；地图页现在也会展示未读的娇娇时间通报 / 系统事件通报，并在通报显示时屏蔽地图点击。
- 修正睡觉收束规则：普通外出行动推进到深夜时保留最后一次深夜行动；深夜行动后或体力归零时，先由娇娇提醒回宫 / 睡觉，再复用寝殿黑屏过夜转场进入次日清晨通报链路。
- 删除独立行动值状态字段和后端旧 runtime schema 中的对应校验，常规行动预算统一只看体力。
- 修复全局对话锁与场景按钮混用时的不可点击问题：侍寝互动和妃嫔会面对白需要先收起，再开放场景内固定操作。
- 修复建章宫太后场景仍被全局对话锁遮挡固定交互的问题：建章宫空闲态不再常驻渲染 `GlobalDialogueStage`，只在送礼问安 / 告辞后进入对话态，并在对话未收束前禁用固定按钮。
- 道具获得 / 失去接入数值 toast 队列，新增物品也会按缺省 0 数量计算增减，不再只提示已有数值字段。
- 修复外景场景侧栏语义：地点内 `外出` 明确返回地图主视角，独立 `回宫` 入口直接回寝殿；在寝殿面板内点击“外出”仍会先关闭面板并展示出行提示，避免转场卡住。
- 新增地点行动统一流程：妙音堂、太医院、御膳房、宝华殿、华清池的耗时行动统一走“行动结果对白 / NPC 对话 -> 深夜或体力归零判定 -> 娇娇提醒 -> 黑屏过夜”链路，不再各自直接 `advanceTime(1)`。
- 修复妙音堂普通听曲后行动结果对白消失的问题；普通地点行动现在复用共享行动结果舞台，数值 toast 随该舞台实际出现触发。
- 更新地点 / 侍寝相关测试预期，并让资源一致性测试支持 URL 编码的 public asset 路径。
- 新增曲谱练习闭环：玩家持有曲谱后可在妙音堂找连翘学谱，学习进度受玩家乐理、曲谱难度和连翘关系影响；曲谱登记宫宴不再消耗库存，练谱提升表现上限，宫宴当天再随机生成本场表现分。
- 修正乐理属性落点：不新增玩家属性字段，继续沿用既有 `talent` 字段承载乐理数值；界面与规则文案显示为“乐理”，读档时仅将历史过渡 `music` 归并回 `talent`。
- 调整学谱成长：每次向连翘学谱会同步增加乐理显示值 `+2`，并进入数值 toast。
- 系统宫宴报名开启调整为宫宴前一个月，首届宫宴会在 1 年 2 月上旬清晨进入报名期，避免开局提醒早于玩家可操作时间。
